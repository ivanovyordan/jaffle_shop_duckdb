select
    1 as customer_id,
    'Peter' as first_name,
    'P.' as last_name

union all
select
    2 as customer_id,
    'Bruce' as first_name,
    'B.' as last_name

union all
select
    3 as customer_id,
    'Tony' as first_name,
    'S.' as last_name